import React, { FunctionComponent } from "react";
import { Header } from "../components/Header";

const Home: FunctionComponent = () => {
  return <Header serviceName={"Matt's Beacon Store"} homeLink={"#"} />;
};

export default Home;
